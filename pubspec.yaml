name: motum_prototype
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_maps_flutter: 2.2.0
  permission_handler: ^10.2.0
  geolocator: ^9.0.2
  device_info_plus: ^3.2.3
  http: ^0.13.5
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true